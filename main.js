import React from "react";
import ReactDOM from "react-dom";

const heading = React.createElement("h2",{id: "line1"},"NamasteReact");
        const root = ReactDOM.createRoot(document.getElementById("heading"));
        root.render(heading);
        